---
import { useTranslations } from "../i18n/utils";
import type { Locale } from "../i18n/ui";

interface Props { raum: "app" | "blog"; lang: Locale; }
const { raum, lang } = Astro.props;
const t = useTranslations(lang);
---
<footer class="foot">
  <div>
    <p class="caps">OE5SOS · Martin Fischer</p>
    <p class="caps num" style="margin:.4rem 0 0">
      {t.footer.ort} · JN67{raum === "blog" ? ` · ${t.footer.portabel}` : ""}
    </p>
  </div>
  <div class="foot__cols">
    <div class="foot__col">
      {raum === "app" ? (
        <>
          <a href="/longpath/#download">{t.footer.appCol.versionen}</a>
          <a href="/longpath/#hardware">{t.footer.appCol.hardware}</a>
          <a href="/longpath/#signalkette">{t.footer.appCol.signalkette}</a>
        </>
      ) : (
        <>
          <a href="/blog/">{t.footer.blogCol.beitraege}</a>
          <a href="/blog/archiv/">{t.footer.blogCol.archiv}</a>
          <a href="/blog/rss.xml">{t.footer.blogCol.rss}</a>
        </>
      )}
    </div>
    <div class="foot__col">
      <a href={raum === "app" ? "/blog/" : "/longpath/"}>{raum === "app" ? t.footer.zumBlog : t.footer.zurApp}</a>
      <a href="/">{t.footer.start}</a>
      <a href="/impressum/">{t.footer.impressum}</a>
    </div>
  </div>
</footer>

<style>
  .foot {
    border-top: 1px solid var(--border-fine);
    margin-top: clamp(3rem, 7vh, 5rem);
    padding-block: clamp(2.2rem, 5vh, 3.5rem);
    display: flex; flex-wrap: wrap; gap: 1.5rem;
    justify-content: space-between; align-items: flex-end;
  }
  .foot__cols { display: flex; flex-wrap: wrap; gap: clamp(1.5rem, 5vw, 3.5rem); }
  .foot__col { display: flex; flex-direction: column; gap: .45rem; }
  .foot__col a {
    font-family: var(--ff-mono); font-size: 9px;
    letter-spacing: .18em; text-transform: uppercase;
    color: var(--t5); transition: color var(--ease);
  }
  .foot__col a:hover { color: var(--t2); }
</style>
